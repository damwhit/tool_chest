<% @tools.each do |tool| %>
 <h3><%= tool.name %> - <%= tool.quantity %> <%= link_to "Link to View #{tool.name}", tool_path(tool.id) %> </h3>
<% end %>
<%= link_to("Create a Tool", new_tool_path) %>

<!-- link_to is link helper, which takes two arguments -->
